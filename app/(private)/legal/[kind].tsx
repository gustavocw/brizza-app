// Route file is THIN: it imports a feature and renders it. No logic here.
// The [kind] param (privacy | terms) is read inside the feature via useLocalSearchParams.
export { default } from '@/features/legal'
